'use client';

/**
 * Legacy export — script loading is handled by PhoenixScriptsProvider + ExtraScripts.
 * Kept so existing shell imports continue to work.
 */
export default function PhoenixScripts() {
  return null;
}
